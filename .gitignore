# === Model weights — hosted on Hugging Face (Kingdrone-Junjue/GeoFM), too large for git ===
# config.py + inference_manifest.json under models/ stay tracked; only the checkpoints are ignored.
*.pth
*.pt
*.ckpt

# === Test embeddings (~37 GB, downloaded from HF at reproduce time — never committed) ===
/emb/
/embeddings/
*.tif
*.tiff

# === Large reproduction artifacts — hosted on Hugging Face (Kingdrone-Junjue/GeoFM), too large for git ===
# soft_master/ (946 .npy, ~950 MB, fast-path input) + final_submission/ (~220 MB LB zip).
/soft_master/
/final_submission/

# === Script outputs (regenerated by reproduce_from_soft.sh / run_full_inference.sh) ===
/submissions/
models/**/predictions/

# === Python ===
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# === Build artifacts (e.g. vendor/ever_src/build — redundant with the shipped wheel + source) ===
build/
dist/

# === Virtual envs ===
.venv/
venv/
env/

# === IDE / OS ===
.idea/
.vscode/
.DS_Store
Thumbs.db
